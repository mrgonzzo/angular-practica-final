 var setBacked = function (array) {

            for (var i = 0; i < array.length; i++) {
                vm.deck[i].stat = 'backed';

            }


            return array;
        };

        vm.goToPlay = function () {
            $state.go('play');
            console.log('recargamos la pagina')
        }

        vm.foo = function () {
            setBacked(vm.deck);
            vm.goToPlay();
            console.log('estado2 ', vm.deck[0].stat, 'foto2: ', vm.deck[0].picture)
        }
